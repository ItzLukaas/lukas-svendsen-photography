/** Web-ready hero loop assets (from `npm run hero:process`). */
export const heroVideoPaths = {
  h264: "/videos/hero.mp4",
  hevc: "/videos/hero-hevc.mp4",
  poster: "/images/hero-poster.jpg",
} as const;
